# PocketBase encryption key - IMPORTANT: Change this to a secure random value in production
PB_ENCRYPTION_KEY=your-secure-encryption-key-change-me

# Scheduler service token for health checks
SERVICE_TOKEN=your-secure-service-token-change-me

# Optional: Node environment (development/production)
NODE_ENV=production
